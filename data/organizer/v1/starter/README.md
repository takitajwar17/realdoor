# RealDoor starter repository

Run `python -m unittest discover -s tests -v` from this directory. The reference code demonstrates deterministic calculations and corpus loading; it is not a production eligibility system. No network call or model SDK is required.
